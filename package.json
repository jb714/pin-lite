{
  "name": "pin-lite",
  "version": "1.0.0",
  "description": "Pin-lite/Pequeño",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "test",
    "heroku-postbuild": "cd pin-lite-client && npm install && npm run build"
  }
}
